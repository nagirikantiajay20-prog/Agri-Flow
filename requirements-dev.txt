-r requirements.txt
pytest>=8.2
pytest-asyncio>=0.23
pytest-cov>=5.0
ruff>=0.5
faker>=25.0
psycopg2-binary>=2.9  # sync driver, needed by scripts/phase0_schema_reconciliation.py and Alembic
fakeredis>=2.23  # in-process Redis for cache / OTP / SSE tests
